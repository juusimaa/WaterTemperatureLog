{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cosmos": {
    "//": "Leave ConnectionString empty to use the in-memory seed data. Set it (emulator or Azure) to use Cosmos DB. In Azure, set it via App Service configuration, not here.",
    "ConnectionString": "",
    "Database": "WaterTemperatures",
    "Container": "Readings"
  }
}
